// Tunable defaults for the UI thread. See spec.md for the reasoning behind
// each value; kept here (rather than inline) so they're easy to revisit.
export const DIFF_THRESHOLD_PERCENT = 0.1;
export const DEFAULT_SCALE = 0.5;
export const SCALE_OPTIONS = [0.25, 0.5, 1] as const;
