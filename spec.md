# Figmaプラグイン仕様書（ドラフト）

プロジェクト名（仮）: **Full Diff Checker**（英語名は検討中。候補: Full Diff Checker / DiffGuard / PixelParity / Whole File Diff Checker）

最終更新: 2026-08-27

## 1. 背景・目的

デザイン仕様書をFigmaで管理・提出しているが、Figma側の仕様上やむを得ず発生する以下の問題に悩まされている。

- メインコンポーネントのバリアント名を変更すると、更新時にインスタンス側の対応が不正になりデフォルトバリアントにリセットされる
- メインコンポーネントの内部構造が変化すると、インスタンス差し替えなどでオーバーライドしていた箇所のオーバーライドがリセットされる

こうした「内部的な取り回し改善（保守性向上）」のための変更は、仕様書の見た目（ユーザーの利益に直結する部分）には影響しないことが前提だが、それを証明するのが難しく、上位レイヤー（仕様書の内容に責任を持つ担当者）から改善提案が通りにくい状況にある。

本プラグインは、Before/After（変更前後、またはブランチ間・バージョン間）の見た目を比較し、「どこが変わっていないか／どこが変わったか」を機械的に一覧化することで、この説明責任を果たすことを目的とする。

既存の「インスタンス更新時に画像比較して差分の有無を判定するプラグイン」とは別の、レビュー・説明責任のためのツールという位置づけ。

## 2. 全体アーキテクチャ

Figma Plugin API単体では実現できない要件（別ファイル／別ブランチ／過去バージョンとの比較）があるため、Plugin APIとREST APIを組み合わせる。

- **Plugin API**: 実行中のファイルのスコープ解決、出力ページ生成（実ノード配置）を担当。今開いているファイルの外には一切アクセスできない。
- **Figma REST API**: Before/After両方の状態を画像としてレンダリング取得するために使用（`GET /v1/files/:key` でページ構成・ノード情報取得、`GET /v1/images/:key` でPNGレンダリング取得）。プラグインUI（iframe、`networkAccess`許可）から呼び出す。

出力は**プラグインウィンドウ内のHTML表示ではなく、比較対象のFigmaファイル内に新規ページを作成し、実ノードとして生成する**（詳細は9章）。プラグインウィンドウは「設定入力」と「実行ログ表示」のみを持つ軽量なコントロールパネルとする。

### なぜ「ノードをそのままコピー」できないか

- Plugin APIは現在開いているファイルのシーングラフしか扱えず、他ファイル・他ブランチ・過去バージョンのノードには一切アクセスできない。
- ブランチも技術的には別ファイル扱いであり、`node.id`はブランチ間で保持されない場合があることを確認済み（republish後にIDが変わる事例あり）。
- インスタンスは「メインコンポーネントの現在の状態」を常に参照するライブな仕組みのため、そもそも過去の状態を凍結したインスタンスとして保持することができない（これは本プラグインが解決しようとしている問題そのもの）。
- REST APIの画像エンドポイントは「Figmaサーバーが代わりにレンダリングした結果（絵）」を返すだけなので、ファイル／ブランチ／バージョンを問わず取得できる。ノードの複製とは難易度が全く異なる。

## 3. 入力仕様（比較対象の指定）

- **Before / After** はそれぞれ以下のいずれかを指定可能:
  - ファイルURL
  - ブランチURL
  - Version History上の特定バージョン
  - 「現在の状態」（Afterのデフォルト）
- ブランチ比較は「オープン中のブランチ」同士のみを前提とする。アーカイブ済み・マージ済みブランチとの比較は行わない（REST APIの`branch_data`がオープン中のブランチしか返さないため）。

## 4. 認証（Figma Personal Access Token）

- REST API呼び出しにはPersonal Access Token（PAT）が必須。スコープ: `file_content:read`, `file_versions:read`。
- Plugin UI（iframe）はFigmaアプリのログインセッションを共有しないため、この認証をスキップする方法はない。
- **初回起動時のみ**、PAT入力用の単独画面（ログイン画面的な位置づけ）を表示する。接続確認後は`figma.clientStorage`（プラグイン＋ユーザー単位のローカル保存）にトークンを保存し、**2回目以降はこの画面をスキップ**して直接スキャン設定画面から開始する。
- トークン失効時のみ再入力を求める。設定画面には「トークンを変更」の導線を常設する。

## 5. スキャン範囲（スコープ）

2つのモードを提供する。

1. **条件で自動スキャン**（デフォルト）
   - **属性を選択**（複数選択可）: メインコンポーネント / インスタンスを含むコンポーネント / セクション / フレーム
   - **階層を選択**: すべての階層 / 第N階層のみ（ページ直下を第1階層とする）
2. **選択中のノードのみ**: Figma上で選択したノードに限定してスキャン（個別の再確認・部分チェック向け）

### 対象外ページ

- Before/After両方のURLを入力すると、**両ファイルのページ一覧の和集合**を自動取得し、除外候補として一覧表示する。
- 片方のファイルにしか存在しないページには「Before限定」「After限定」の目印を付ける。
- 初期状態では除外せず一覧に残す（ページごと削除・追加された可能性に気づけるようにするため）。

## 6. レンダリング仕様

- **形式はPNGに統一**（表示・差分算出とも）。
  - SVGは以下の理由で不採用:
    - 角度（コニック）グラデーションがSVGでは非対応で、Figmaが強制的にradialグラデーションへ変換し見た目が変わる
    - 複数SVGを同時に扱うとグラデーションIDが衝突し破損する事例がある
    - `figma.createNodeFromSvg()`でベクターノード化する場合、無数のインスタンスを含む巨大フレームほどノード数が肥大化し、PNGのような解像度キャップによる歯止めが効かない。処理の安定性という観点でもPNGより不利。
  - 実体コピー（インスタンス）案は2章の理由により技術的に不可。
- **レンダリングスケール**: 0.25x / 0.5x（デフォルト） / 1x から選択（1xより大きい値は用途上不要なため提供しない）。
- 極端に大きいノードは内部で**長辺4096px相当に自動キャップ**する。
  - 根拠: Figmaの画像フィル（`figma.createImage()`）自体がPNG/JPEG/GIFで4096×4096pxを上限としているため、書き出し時点でこれに収める。
  - Before/Afterは通常同寸法のため、同じキャップ率が両方にかかり重ね合わせのズレは発生しない。

## 7. 差分検出・判定ロジック

- **しきい値は0.10%で固定**。プロジェクトごとの可変設定は設けない（アンチエイリアシング誤検知対策としての一般的な値を採用）。
- **許容grainは設けず、しきい値超はすべて厳格に「差分あり」として扱う**二値判定。中間的な「要確認」レベルは設けない。
- 判定用の数値（非黒ピクセル率）は参考値であり、主たる判断材料は8章の差分合成画像（目視でどこが変わったか分かる）とする。

## 8. 差分の可視化

- Beforeの画像の上にAfterの画像を重ね、Figma標準の**ブレンドモード `blendMode: "DIFFERENCE"`** を適用した2レイヤーとして生成する。
- 完全一致なら黒一色、差分がある箇所は色が浮かび上がるため、目視でそのまま「どこが変わったか」を判断できる。
- クライアント側で計算した画像を貼り付けるのではなく、Figma自身が本番でも同じ見え方でレンダリングする点がポイント（ズーム・パンしても正しく見える）。
- 非黒ピクセル率（＝差分ピクセルの割合）をしきい値判定・ソート用の参考数値として添える。

## 9. 対応付け（マッチング）ロジック

Before/Afterでページ構成・フレーム構成・対象件数が異なる場合の扱い。

- **メインコンポーネント**: `component.key` で対応付ける（ブランチ間・republish後も安定することを確認済み）。
- **セクション/フレーム**: 安定した識別子がないため「名前＋階層パス」で対応付ける（`node.id`はブランチ間で変わり得るため使用しない）。リネームされた場合は別物として検出される限界があるが、他の差分ツール一般に共通する制約として許容する。
- 対応関係は3種類に分類する:
  1. **対応あり**（Before/After双方に存在）: 通常のBefore/After/差分の行として表示。Beforeの並び順を基準に一覧化する。
  2. **Beforeのみ（削除）**: 🗑マークを付け、元の位置に残す形で一覧に含める（差分の一種として明示的に見せる。厳格な差分検知の方針上、消失も見逃さない）。
  3. **Afterのみ（新規追加）**: ✨マークを付け、一覧の末尾にまとめて表示する。

## 10. 出力仕様（生成されるFigmaページ）

- スキャン完了後、**比較対象のFigmaファイル内に新規ページ**（例: 「[プラグイン名] Report — 2026-08-27 15:41」）を自動生成する。
- 各行はFigmaの実ノードとして構築する:
  - ステータス（差分あり/差分なし/削除/新規追加）
  - ノード名・パス
  - Before画像フィル（`figma.createImage()`で埋め込み）
  - After画像フィル
  - 差分合成レイヤー（8章参照）
  - 非黒ピクセル率
- Auto Layoutで縦方向に並べる想定（詳細な行の高さ・余白等は今後詰める）。
- 無数のインスタンスを含む巨大フレームでも、Figmaネイティブのズーム・パン・コメント機能がそのまま使える。

## 11. UI構成（プラグインウィンドウ）

3画面構成。

1. **初回のみ（ログイン画面）**: PAT入力 → 接続確認 → `clientStorage`に保存。2回目以降はスキップ。
2. **スキャン設定**: Before/After入力、スコープ選択、対象外ページ選択、レンダリング設定、スキャン開始ボタン。認証状態を示す小さなステータス行を上部に表示。
3. **実行ログ**: スキャンの進捗をテキストログで表示。完了後「新規ページを作成しました」＋「ページを開く」ボタンを表示（結果テーブルそのものはプラグインウィンドウ内に表示しない）。

## 12. 制約・既知の限界

- Figma REST API Tier1エンドポイント（files / images）のレート制限は、Proプラン Full/Dev seatで15 req/min。ノードIDを`ids`パラメータへ一括指定してバッチ化し、リクエスト数を最小化する。
- ブランチ比較はオープン中のブランチ同士のみ。マージ済み・アーカイブ済みブランチとの遡及比較は不可。
- セクション/フレームの対応付けは名前ベースのため、リネームを伴う変更は「削除＋新規追加」として検出される。
- `figma.clientStorage`に保存したトークンは、技術的に十分な労力をかければ同一クライアント上のユーザーから読み取り得る（外部攻撃者からの保護ではなく、通常のローカル保存として扱う）。

## 13. 未決事項

- プラグイン名の正式決定（英語名候補: Full Diff Checker / DiffGuard / PixelParity / Whole File Diff Checker）
- 生成ページのAuto Layout詳細（1行の高さ、余白、ページが複数枚にまたがる場合の分割方法など）
- CSV書き出し・レポートリンク発行機能の要否
- スキャン設定の保存・再利用（「設定を保存」ボタンの永続化先）
