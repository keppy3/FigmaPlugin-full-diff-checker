node_modules/
*.log
.DS_Store

# dist/ is intentionally committed (not ignored): reviewers/testers open this
# repo without Node/npm installed, so it needs to run straight off a clone or
# ZIP download with no build step. Rebuild and commit dist/ after src changes.
